import {} from './actions';

const initialState = {
  currentSessions: null,
};

export default (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
}
